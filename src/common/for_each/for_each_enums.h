#ifndef __FOR_EACH_ENUMS_H__
#define __FOR_EACH_ENUMS_H__

#include "for_each_config.h"

enum t_for_each_type { FET_SERIAL_CPU, FET_CUDA, FET_OPENMP };
typedef t_for_each_type t_for_each_ndim_type;
//enum t_for_each_ndim_type { FET_SERIAL_CPU, FET_CUDA, FET_OPENMP };

#endif
